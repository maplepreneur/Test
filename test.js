const name = 'World';
console.log(`Hello, ${name}!`);

// This is a simple JavaScript file that defines a variable `name` with the value 'World' and then logs a greeting message to the console using template literals. When you run this code, it will output: "Hello, World!"
